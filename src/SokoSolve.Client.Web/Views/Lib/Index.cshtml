@model SokoSolve.Core.Lib.LibraryCollection

@{
    ViewBag.Title = "Library Collection";
}

<h2>@ViewBag.Title</h2>

<ul>
    @foreach (var i in Model.IdToFileName.Keys)
    {
        <li><a asp-action="Home" asp-route-id="@i">@i</a></li>
    }
</ul>
