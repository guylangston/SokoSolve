@using System.Numerics
@using SokoSolve.Core
@using SokoSolve.Drawing
@model SokoSolve.Core.Puzzle
@{
    var dia = new PuzzleDiagram()
    {
        GetResource = x => "/img/"+x
    };
    dia.Draw(Output, Model, new Vector2(16));
}

